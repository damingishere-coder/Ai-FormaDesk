# 贡献指南

请先通过 Issue 描述问题或改进目标。错误报告建议包含系统与应用版本、复现步骤、预期结果和实际结果；不要上传认证文件、Cookie、Token、私人作品或完整运行目录。

开发需要 Node.js 22+；桌面打包需要 Apple Silicon Mac。先运行 `npm ci`，修改后运行 `npm test` 与 `npm run build`。桌面改动另运行 `npm run desktop:prepare` 与 `npm run test:desktop`。

真实 AI 验证可能消耗账户额度；仅在自己明确知情的环境执行。测试应使用独立数据目录，不能操作真实用户作品。保持失败任务不覆盖成功版本、对象 ID 稳定、本机执行与认证隔离。

PR 请写明用户能观察到的行为变化、验证结果和限制。不要提交 `data`、`.env`、日志、缓存、安装包或生成的测试产物。发布包上传到 GitHub Releases。

本仓库尚未指定项目级开源许可证；请勿把源码公开可见等同于已经授予任意再分发许可。第三方依赖遵循各自许可证。
